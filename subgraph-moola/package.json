{
  "name": "Raise",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ pierrot498/Raise",
    "create-local": "graph create --node http://localhost:8020/ Raise",
    "remove-local": "graph remove --node http://localhost:8020/ Raise",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Raise",
    "test": "graph test",
    "coverage": "graph test -c"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.57.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
